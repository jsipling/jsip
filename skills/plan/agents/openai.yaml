interface:
  display_name: "JSIP Plan"
  short_description: "Derive a test-first plan from an approved spec"
  default_prompt: "Use $plan to derive an implementation plan from my approved JSIP specification."
policy:
  allow_implicit_invocation: false
